@model SD_Turizm.Web.Models.DTOs.SaleDto

@{
    ViewData["Title"] = "Satış Düzenle";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4 mt-4">
        <div class="d-flex align-items-center">
            <div class="bg-gradient-primary rounded-circle p-3 me-3 shadow-sm">
                <i class="fas fa-edit fa-2x text-white"></i>
            </div>
            <div>
                <h1 class="h2 mb-0 text-dark fw-bold">Satış Düzenle</h1>
                <p class="text-muted mb-0">Satış bilgilerini güncelleyin</p>
            </div>
        </div>
        <a asp-action="Index" class="btn btn-secondary btn-lg shadow-sm">
            <i class="fas fa-arrow-left me-2"></i>Geri Dön
        </a>
    </div>
    
    <div class="card shadow">
        <div class="card-header bg-primary text-white">
            <h6 class="mb-0"><i class="fas fa-shopping-cart"></i> Satış Bilgileri</h6>
        </div>
        <div class="card-body">
            <form asp-action="Edit" class="needs-validation" novalidate>
                <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>
                <input type="hidden" asp-for="Id" />
                
                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label asp-for="SaleNumber" class="form-label"><i class="fas fa-receipt"></i> Satış Numarası</label>
                        <input asp-for="SaleNumber" class="form-control" required />
                        <span asp-validation-for="SaleNumber" class="text-danger small"></span>
                    </div>
                    
                    <div class="col-md-6 mb-3">
                        <label asp-for="SaleDate" class="form-label"><i class="fas fa-calendar"></i> Satış Tarihi</label>
                        <input asp-for="SaleDate" class="form-control" type="date" required />
                        <span asp-validation-for="SaleDate" class="text-danger small"></span>
                    </div>
                </div>
                
                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label asp-for="CustomerName" class="form-label"><i class="fas fa-user"></i> Müşteri Adı</label>
                        <input asp-for="CustomerName" class="form-control" required />
                        <span asp-validation-for="CustomerName" class="text-danger small"></span>
                    </div>
                    
                    <div class="col-md-6 mb-3">
                        <label asp-for="TotalAmount" class="form-label"><i class="fas fa-money-bill-wave"></i> Toplam Tutar</label>
                        <input asp-for="TotalAmount" class="form-control" type="number" step="0.01" required />
                        <span asp-validation-for="TotalAmount" class="text-danger small"></span>
                    </div>
                </div>
                
                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label asp-for="Currency" class="form-label"><i class="fas fa-coins"></i> Para Birimi</label>
                        <select asp-for="Currency" class="form-select" required>
                            <option value="">Para Birimi Seçin</option>
                            @if (ViewBag.Currencies != null)
                            {
                                @foreach (var currency in ViewBag.Currencies)
                                {
                                    <option value="@currency.GetProperty("code").GetString()">
                                        @currency.GetProperty("flag").GetString() @currency.GetProperty("code").GetString() - @currency.GetProperty("name").GetString()
                                    </option>
                                }
                            }
                        </select>
                        <span asp-validation-for="Currency" class="text-danger small"></span>
                    </div>
                    
                    <div class="col-md-6 mb-3">
                        <label asp-for="Status" class="form-label"><i class="fas fa-info-circle"></i> Durum</label>
                        <select asp-for="Status" class="form-select" required>
                            <option value="">Durum Seçin</option>
                            @if (ViewBag.SaleStatuses != null)
                            {
                                @foreach (var status in ViewBag.SaleStatuses)
                                {
                                    <option value="@status.GetProperty("code").GetString()">@status.GetProperty("name").GetString()</option>
                                }
                            }
                        </select>
                        <span asp-validation-for="Status" class="text-danger small"></span>
                    </div>
                </div>
                
                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-primary"><i class="fas fa-save"></i> Kaydet</button>
                    <a asp-action="Index" class="btn btn-secondary"><i class="fas fa-times"></i> İptal</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        // Bootstrap 5 form validation
        (function() {
            'use strict';
            window.addEventListener('load', function() {
                var forms = document.getElementsByClassName('needs-validation');
                var validation = Array.prototype.filter.call(forms, function(form) {
                    form.addEventListener('submit', function(event) {
                        if (form.checkValidity() === false) {
                            event.preventDefault();
                            event.stopPropagation();
                        }
                        form.classList.add('was-validated');
                    }, false);
                });
            }, false);
        })();
    </script>
} 
