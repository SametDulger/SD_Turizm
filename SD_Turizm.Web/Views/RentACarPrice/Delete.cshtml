@model SD_Turizm.Web.Models.DTOs.RentACarPriceDto

@{
    ViewData["Title"] = "Araç Kiralama Fiyatı Sil";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4 mt-4">
        <div class="d-flex align-items-center">
            <div class="bg-gradient-danger rounded-circle p-3 me-3 shadow-sm">
                <i class="fas fa-trash fa-2x text-white"></i>
            </div>
            <div>
                <h1 class="h2 mb-0 text-dark fw-bold">Araç Kiralama Fiyatı Sil</h1>
                <p class="text-muted mb-0">Araç kiralama fiyat kaydını silin</p>
            </div>
        </div>
        <a asp-action="Index" class="btn btn-secondary btn-lg shadow-sm">
            <i class="fas fa-arrow-left me-2"></i>Geri Dön
        </a>
    </div>
    
    <div class="alert alert-warning" role="alert">
        <h4 class="alert-heading">
            <i class="fas fa-exclamation-triangle me-2"></i>Bu araç kiralama fiyatını silmek istediğinizden emin misiniz?
        </h4>
        <p class="mb-0">Bu araç kiralama fiyatını silmek istediğinizden emin misiniz? Bu işlem geri alınamaz.</p>
    </div>
    
    <div class="card shadow mb-4">
        <div class="card-header py-3 bg-danger text-white">
            <h6 class="m-0 fw-bold">
                <i class="fas fa-info-circle me-2"></i> Silinecek Araç Kiralama Fiyatı Bilgileri
            </h6>
        </div>
        <div class="card-body">
            <div class="row">
                <div class="col-md-6">
                    <dl class="row">
                        <dt class="col-sm-4"><i class="fas fa-car"></i> Araç Kiralama</dt>
                        <dd class="col-sm-8">@ViewBag.RentACarName</dd>
                        
                        <dt class="col-sm-4"><i class="fas fa-car-side"></i> Araç Tipi</dt>
                        <dd class="col-sm-8">@Model.CarType</dd>
                        
                        <dt class="col-sm-4"><i class="fas fa-dollar-sign"></i> Fiyat</dt>
                        <dd class="col-sm-8">@Model.Price.ToString("N2") @Model.Currency</dd>
                        
                        <dt class="col-sm-4"><i class="fas fa-calendar"></i> Tarih</dt>
                        <dd class="col-sm-8">@Model.Date.ToShortDateString()</dd>
                    </dl>
                </div>
                <div class="col-md-6">
                    <dl class="row">
                        <dt class="col-sm-4"><i class="fas fa-user"></i> Yetişkin Fiyatı</dt>
                        <dd class="col-sm-8">@Model.AdultPrice.ToString("N2") @Model.Currency</dd>
                        
                        <dt class="col-sm-4"><i class="fas fa-child"></i> Çocuk Fiyatı</dt>
                        <dd class="col-sm-8">@Model.ChildPrice.ToString("N2") @Model.Currency</dd>
                        
                        <dt class="col-sm-4"><i class="fas fa-baby"></i> Bebek Fiyatı</dt>
                        <dd class="col-sm-8">@Model.InfantPrice.ToString("N2") @Model.Currency</dd>
                    </dl>
                </div>
            </div>
        </div>
    </div>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <div class="d-flex gap-2">
            <button type="submit" class="btn btn-danger">
                <i class="fas fa-trash"></i> Sil
            </button>
            <a asp-action="Index" class="btn btn-secondary">
                <i class="fas fa-times"></i> İptal
            </a>
        </div>
    </form>
</div> 