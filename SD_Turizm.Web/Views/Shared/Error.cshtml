@model ErrorViewModel
@{
    ViewData["Title"] = "Hata";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4 mt-4">
        <div class="d-flex align-items-center">
            <div class="bg-gradient-danger rounded-circle p-3 me-3 shadow-sm">
                <i class="fas fa-exclamation-triangle fa-2x text-white"></i>
            </div>
            <div>
                <h1 class="h2 mb-0 text-dark fw-bold">Hata</h1>
                <p class="text-muted mb-0">Bir hata oluştu, lütfen tekrar deneyin</p>
            </div>
        </div>
    </div>
    
    <div class="card shadow mb-4">
        <div class="card-header py-3 bg-danger text-white">
            <h6 class="m-0 fw-bold">
                <i class="fas fa-exclamation-triangle me-2"></i>Bir hata oluştu
            </h6>
        </div>
        <div class="card-body">
            <h2 class="text-danger">İsteğiniz işlenirken bir hata oluştu.</h2>

            @if (Model.ShowRequestId)
            {
                <div class="alert alert-info">
                    <strong>İstek ID:</strong> <code>@Model.RequestId</code>
                </div>
            }

            <div class="alert alert-warning">
                <h5><i class="fas fa-code"></i> Geliştirme Modu</h5>
                <p class="mb-0">
                    <strong>Development</strong> ortamına geçiş yaparak, oluşan hata hakkında daha detaylı bilgi görüntüleyebilirsiniz.
                </p>
            </div>
            
            <div class="alert alert-info">
                <p class="mb-0">
                    <strong>Development ortamı dağıtılan uygulamalar için etkinleştirilmemelidir.</strong>
                    Bu, istisnalardan gelen hassas bilgilerin son kullanıcılara görüntülenmesine neden olabilir.
                    Yerel hata ayıklama için, <strong>ASPNETCORE_ENVIRONMENT</strong> ortam değişkenini <strong>Development</strong> olarak ayarlayın
                    ve uygulamayı yeniden başlatın.
                </p>
            </div>
        </div>
    </div>
</div>
